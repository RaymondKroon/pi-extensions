The Ralph loop was interrupted and is now resumed by the user's message. The user has extra info for the loop — take it into account. Continue the current iteration exactly where the interrupted turn left off.

User's extra info:
{{extraInfo}}
