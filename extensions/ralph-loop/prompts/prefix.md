---
description: Prefix for every prompt Ralph injects as a user message — makes the sender explicit so the model does not misattribute these to the human user.
example_input: |
  {}
---
[Automated Ralph loop instruction, not from the human user]
